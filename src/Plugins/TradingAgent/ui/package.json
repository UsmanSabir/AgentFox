{
  "name": "agentfox-trading-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Web UI for the AgentFox TradingAgent plugin. Deliberately a SEPARATE npm project from src/frontend so trading-only dependencies (charting) never enter the host app.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.0.0",
    "svelte-check": "^4.1.0",
    "typescript": "^5.7.3",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "lucide-svelte": "^0.468.0"
  }
}
