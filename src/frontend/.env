# Backend URL used by the Vite dev proxy (NOT exposed to browser bundles).
# Override per-machine in .env.local (git-ignored).
#
# dotnet run  →  http://localhost:5000  (ASP.NET Core default)
# Service     →  http://localhost:8080  (Services.Port in appsettings.json)
BACKEND_URL=http://localhost:8080
